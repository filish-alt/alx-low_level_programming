#include "main.h"

/**
 * _isupper - return 1 if uppercase and 0 otherwise
 * @c: take in character
 * Return: Always 0.
 **/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
