#include "main.h"

/**
 * _isdigit - return 1 if its digit and 0 otherwise
 * @c: take in integet
 * Return: Always 0.
 **/
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
