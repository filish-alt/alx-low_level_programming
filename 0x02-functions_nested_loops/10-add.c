#include "main.h"
/**
 * add - function that adds two integers together
 * @a: takes in an integer
 * @b: takes in an integer
 * Return: a + b result
 */
int add(int, int)
{
int i, j,add;
add = i + j;
return (add);
